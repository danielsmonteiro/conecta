{
  "name": "healthmatch-recovery",
  "version": "1.0.0",
  "private": true,
  "description": "Ferramenta de recuperacao tecnica (read-only) do frontend e mapeamento de APIs do HealthMatch.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "playwright install chromium",
    "recover": "tsx src/recover.ts",
    "interactive": "tsx src/interactive.ts",
    "report": "tsx src/report.ts",
    "clean": "rm -rf recovered-healthmatch"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
